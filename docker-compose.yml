version: '3.8'

services:
  dr_api:
    build: .
    image: dr-api:latest
    container_name: dr_api
    ports:
      - "8000:8000"
    restart: always
    environment:
      - WORKERS=4
      - BIND=0.0.0.0:8000
